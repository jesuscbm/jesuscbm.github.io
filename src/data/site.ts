import type { GlyphName } from "@/lib/glyphs";

// The live mark, used everywhere (navbar, footer, favicon, loading animation).
// Compare all options at /logos/, then set the name here.
// Compare all families at /logos/ (Abstract · Greek · Pixel · Figures), then
// set the chosen name here. Full list lives in src/lib/glyphs.ts (GLYPHS).
export const MARK: GlyphName = "cinb";

export const SITE_URL = "https://www.jesusblazquez.eu";
